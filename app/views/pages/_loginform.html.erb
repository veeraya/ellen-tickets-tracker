<%= form_tag("/login", :method => "post", :remote => "true", :id => "loginform") do %>
  <legend id="legend">Get an email alert when new tickets become available</legend>
  <%= text_field_tag(:email, nil, :placeholder => "Email") %><br />
  <%= submit_tag("Start tracking", :class=> "btn btn-primary") %>
<% end %>